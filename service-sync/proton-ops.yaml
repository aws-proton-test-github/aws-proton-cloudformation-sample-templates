sync:
  services:
    service-crud:
      alpha:
        branch: dev
        spec: service-sync/dev/service-sync-dev.yaml
      beta:
        branch: dev
        spec: service-sync/dev/service-sync-dev.yaml
