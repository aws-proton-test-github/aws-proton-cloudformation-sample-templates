sync:
  services:
    service-crud:
      alpha:
        branch: main
        spec: service-sync/dev/service-sync-dev.yaml
      beta:
        branch: main
        spec: service-sync/dev/service-sync-dev.yaml
