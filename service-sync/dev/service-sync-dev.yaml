proton: ServiceSpec
instances:
  - name: alpha
    environment: lambda-env
    spec:
      resource_name: "task"
      resource_handler: "src/api"
      lambda_memory: 512
      lambda_timeout: 29
      lambda_runtime: "ruby2.7"
  - name: beta
    environment: lambda-env
